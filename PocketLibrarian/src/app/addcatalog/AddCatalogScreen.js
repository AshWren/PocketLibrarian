/*  

Screen that houses form to add a new catalog to put items in

Wants: Presets available based on properties added by user for other catalogs 
--> Will prevent typos/difficult filtering for property between catalogs


*/

import React, { Component } from 'react';
import { Text, View } from 'react-native';

class AddCatalogScreen extends Component {
    render() {
        return (
            <View>
                <Text>AddCatalogScreen Works!</Text>
            </View>
        );
    }
}

export default AddCatalogScreen;
